import { Dispatch, SetStateAction } from 'react';
type RevokeModalProps = {
    revokeModalOpen: boolean;
    setRevokeModalOpen: Dispatch<SetStateAction<boolean>>;
};
export declare const RevokeModal: ({ revokeModalOpen, setRevokeModalOpen }: RevokeModalProps) => import("react/jsx-runtime").JSX.Element;
export {};
